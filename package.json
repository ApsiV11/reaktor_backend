{
  "name": "reaktor_backend",
  "version": "1.0.0",
  "description": "Backend for Reaktor Summer Developer 2022 pre-assigment",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon app.js",
    "build:ui": "del public && cd ../reaktor && npm run build && xcopy build ..\\reaktor_backend\\public\\ /e /s /y",
    "deploy:heroku": "git checkout heroku && git push heroku",
    "deploy": "git checkout master && git push master",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && npm run deploy"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ApsiV11/reaktor_backend.git"
  },
  "author": "ApsiV11",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/ApsiV11/reaktor_backend/issues"
  },
  "homepage": "https://github.com/ApsiV11/reaktor_backend#readme",
  "dependencies": {
    "axios": "^0.24.0",
    "dotenv": "^10.0.0",
    "express": "^4.17.2",
    "express-ws": "^5.0.2",
    "mongoose": "^6.1.4",
    "mongoose-unique-validator": "^3.0.0",
    "websocket": "^1.0.34"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
